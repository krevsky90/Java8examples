public class LambdaAndDefaultInterfaceMethod {
    void cannotUseDefaultMethod() {
        /**
         * you can't use default interface methods inside lambda-methods
         */
        // Formula f = a -> {sqrt(a*100)};
    }
}
